model Category {
    id        String   @id @default(cuid())
    name      String   @unique
    keywords  String[]
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt @default(now())
 
    offers Offer[]
    @@index([name])
 }