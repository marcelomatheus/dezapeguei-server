model Wishlist {
  id        String   @id @default(cuid())
  name      String
  userId    String @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  user User @relation("user_wishlist", fields: [userId], references: [id])

  @@index([userId])
}